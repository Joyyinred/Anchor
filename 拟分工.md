# Anchor — 技术栈 & 25 天 / 2 人分工（定稿 v2）

> AI Builders Hackathon · Anchor 专注桌宠 · 执行计划
> 约束：2 人 · 25 天（8/21–9/15）· 目标是「核心验证尽早、demo/deck 全程有位、绝不最后三天突击」

---

## 0. 这版相比初稿改了什么

1. **把验证从 Day 12 提前到 Day 5**：用纯 mock 数据先验证引擎逻辑（不接浏览器），逻辑对不对第 5 天就见分晓，而不是接了真信号才发现判定式有问题、然后返工。
2. **给 deck + demo 开一条从 Day 11 起的常设线**：demo 视频 + pitch deck 占评审约 30%，且是硬性提交物，绝不能塞到最后三天由半个人负责。
3. **砍掉 MVP 阶段的 Supabase，纠正 A/B 角色**：存储用 `chrome.storage.local` 即可跑通 demo；A=前端/部署，B=AI 核心（引擎深化）。

---

## 1. 技术栈

| 模块 | 技术 | 说明 |
|---|---|---|
| 插件形态 | Chrome 扩展，Manifest V3 | `load unpacked`，demo 不需上架审核；平台风险由 A 从 Day 1 单独扛 |
| 前端 UI | React + Tailwind + shadcn/ui | side panel / content script 浮层渲染桌宠与设置界面 |
| 信号采集 | `chrome.tabs`、`chrome.idle`、Page Visibility API | 全部是扩展权限内的原生能力，零后端成本 |
| **检测引擎** | **纯 TypeScript 独立模块，不依赖 Chrome API** | 输入信号事件 → 输出动作（DO_NOTHING/CHECK_IN/MICRO_RESTART），可脱离插件单独测。**简历上最值钱、也是评委最认的部分，务必和插件壳彻底解耦** |
| 状态机 | 手写状态机或 XState | 建模「陪伴中 / 观察中 / check-in 中」几种状态，比一堆 if-else 更好在 deck 里讲清设计 |
| LLM 调用 | Anthropic API | 仅两处用途：①起步教练拆解出第一步建议（单次调用，不做多轮）②域名-任务相关性分类（**异步 + 惰性 + 缓存**，绝不阻塞引擎，见 §5 稳定性红线） |
| 分类缓存 | `chrome.storage.local` 或内存 Map | 每域名每会话只判一次；缓存命中率可量化成一个数字进 Q&A 弹药库 |
| 存储 | **`chrome.storage.local`（MVP 就用它）** | 任务/会话/锚点/白名单/check-in 反应，本地存足够演。**Supabase 挪到 Roadmap**——它服务的是手机同步/跨设备，不是 MVP |
| 悬浮呈现（第二阶段，有余量才做） | `documentPictureInPicture` API | 纯浏览器原生，不需 Electron/Tauri；核心逻辑跑稳后再加 |
| 桌面全局 / 手机 companion | —— | **暂不做，进 Roadmap**。桌面全局要系统权限、多 OS 适配，25 天内难完成 |

**架构主线一句话**：一个纯 TS 引擎（吃事件、吐动作）+ 一个 MV3 插件壳（读真实信号、渲染桌宠）。两者只通过一个约定好的事件 schema 相连——这条解耦缝是整个分工能并行的前提。

---

## 2. 两人角色

- **A ｜ 前端 + 平台 + 部署 + Demo**：MV3 扩展骨架、信号采集、桌宠 UI 与动画、side panel/悬浮呈现、demo 录制。契合有 kiosk 系统经验的一方。
- **B ｜ AI 核心**：检测引擎（四信号计算 + profile 静音 + 置信度判定式 + 自适应退让）、起步教练拆解 prompt、域名分类逻辑、check-in/微重启措辞。**这是技术灵魂，最难，第一天就开始。**

---

## 3. 阶段一（Day 1–10）：验证「聪明的不打扰」——尽早排掉两类风险

**核心原则**：真正互相牵动、值得两人结对的，只有「信号定义 + 评分逻辑」那几天；**平台风险（MV3）和逻辑风险（判定准不准）应当解耦并行**。

### Day 1–2 ｜ 共定契约（唯一必须两人现场对齐的事）

- 一起敲定 `SignalEvent` 与 `DetectionResult` 两个接口——这是后面所有代码的契约，现场过一遍达成一致。
- 约定好 `events.json` 假数据的 schema（A 将来喂真事件、B 现在喂假事件，两边同一个入口）。
- 定完立刻分头，进入并行。

### Day 3–5 ｜ 并行：B 攻引擎逻辑，A 攻 MV3 壳

**B（引擎，纯 mock 驱动，不碰浏览器）**
- 写 `SessionContext` + 四信号计算 + `applyProfileMuting` + `isDrifting` 判定式 + check-in/退让逻辑。
- 同步写测试：拿真实场景编几十组 mock 信号序列，验证误报/漏报。

**A（插件壳，与逻辑无关，可独立推进）**
- MV3 扩展骨架 + 权限模型 + service worker 生命周期跑通。
- 读到「当前活跃 tab 域 / 切换序列 / idle」并转成 `SignalEvent` 格式。
- side panel 里渲染一个最简桌宠（能表达「安静陪伴 / 提醒」两种状态即可）。

> **★ Day 5 硬检查点★**
> B 的引擎在纯 mock 数据下，下面五条必须全绿：
> 1. 编程画像 · IDE↔docs↔AI↔SO 反复快切（全相关域）→ **全程 DO_NOTHING**（★核心：切换不误报）
> 2. 深读画像 · 同一篇 20 分钟没切没动 → **DO_NOTHING**（深读不误报）
> 3. 编程画像 · 飘到 youtube + 锚点 10min 没碰 + 被动刷 → **CHECK_IN**（真走神抓得到）
> 4. 上一条用户答「我在查资料」→ **DO_NOTHING 且此后该域不再报**（自适应白名单生效）
> 5. 开场 2 分钟就切无关域 → **DO_NOTHING**（宽限期生效）
>
> **五条不全绿，不进 Day 6，就地打磨判定逻辑。** 这一步只花假数据、不花时间接浏览器，是最便宜的验证。

### Day 6–8 ｜ 合流：把真实信号接进引擎，复现两个反差瞬间

- A 的真实 `SignalEvent` 流替换掉 mock，喂进 B 的引擎——因为入口 schema 早已约定，**引擎逻辑一行不用改**。
- 一起跑通「插件里真实触发一次 check-in」的完整闭环。这一步 MV3 平台坑最多（权限、生命周期），两人一起攻更快。

> **★ Day 8 硬检查点★**
> 在真实浏览器里，能否稳定复现两个反差瞬间：**疯狂切 tab 不打扰 + 飘去无关网站触发 check-in**。跑不稳 → 继续加时间打磨，**不进入 Day 9**。

### Day 9–10 ｜ 接起步教练，打通端到端

- 接入功能一最小版（单次 LLM 调用生成第一个物理动作建议）。
- 确认它产出的「任务声明 / profile / 锚点」能正确喂给检测引擎——这是两个功能的接口，两人都要清楚。
- 端到端跑通：起步 → 陪伴 → 真走神拉回 → 收尾一句反思。

---

## 4. 阶段二（Day 11–25）：核心稳了，三条线并行

接口已稳定，剩下模块彼此独立。**关键变化：从 Day 11 起就有一条常设的 deck/demo 线，不是最后三天突击。**

### 线一 ｜ B：引擎深化 + AI 打磨（产品护城河）

- Day 11–14：自适应退让的单会话启发式（据用户反应调阈值）、域名分类的异步+缓存优化。
- Day 15–18：check-in 与微重启措辞反复调（**像朋友不像监工**，这是 demo 成败点之一）、起步拆解 prompt 打磨。
- Day 19–22：（有余量才做）`documentPictureInPicture` 悬浮桌宠。
- Day 23–25：联调、修 bug、备好技术 Q&A 弹药（误报率、缓存命中率等可量化数字）。

### 线二 ｜ A：呈现层 + 收尾体验（demo 的脸面）

- Day 11–14：桌宠动画完善（陪伴中 / 观察中 / check-in 三态）、check-in 交互 UI。
- Day 15–18：收尾反思视图、角落专注时长显示（**配角，别喧宾夺主**）、整体视觉打磨。
- Day 19–22：优雅降级处理（信号异常时不崩、给友好提示）、部署与安装说明。
- Day 23–25：与线三合流，主攻 demo 录制。

### 线三 ｜ 两人共担：deck + demo（占约 30% 分，从 Day 11 就滚动推进）

- Day 11 起：建一份 deck 骨架（大纲见配套文档），**每周往里填一次真实进展截图/数据**，别等最后写。
- Day 15 左右：初版 demo 脚本定稿，开始按脚本走查，暴露「哪个反差瞬间演不稳」并反推工程去补。
- Day 20–23：**录制备用 demo 视频**——现场信号触发有随机性，务必有一条提前录好、剪好的稳定版兜底。
- Day 24–25：deck 定稿 + demo 视频定稿 + repo README/文档补全（提交硬性要求）。

> **每日 15–20 分钟小会**：三条线并行时最容易「各自封闭到最后发现接口不匹配」。每天对齐一次进度与接口，成本极低、回报极高。

---

## 5. 三条不可逾越的稳定性红线（demo 翻车预防）

1. **域名分类绝不阻塞引擎**：LLM 分类是**异步**的，没判出来前一律按 `UNKNOWN` 保守处理（即不触发 check-in），判完写缓存。demo 前把要演的几个域名**预热进缓存**。同步现算 = 网络一慢介入时机就飘 = 你精心排的反差瞬间栽在网络上。
2. **必有本地黑白名单兜底**：断网 / API 失败时，靠内置的娱乐域黑名单 + 会话白名单也能给出合理判断，绝不因为 LLM 挂了就整个哑掉。
3. **必录备用 demo 视频**：现场实时触发信号有随机性，提前录一条剪好的稳定版，是对 15% 演示分的保险。

---

## 6. 关键里程碑一览
| 日期 | 里程碑 | 性质 |
|---|---|---|
| Day 2 | `SignalEvent` / `DetectionResult` 契约敲定 | 契约 |
| **Day 5** | **引擎 5 条 mock 测试全绿** | **★关键点一★** |
| **Day 8** | **真实浏览器复现两个反差瞬间** | **★关键点二★** |
| Day 10 | 起步→陪伴→拉回→反思 端到端跑通 | 闭环 |
| Day 11 | deck/demo 常设线启动 | 交付物 |
| Day 15 | demo 脚本初稿定稿、开始走查 | 交付物 |
| Day 22 | 备用 demo 视频录制完成 | 保险 |
| Day 25 | repo + 文档 + deck + demo 全部定稿提交 | 终点 |

---

## 7. 总结

> **" Day 5 用假数据、Day 8 用真信号，两次证明『编程切换时闭嘴、真走神时开口』这个核心区分是稳的。前 8 天就是全部风险所在"**

> 两次关键检验都过 → 这个产品成立，后面是执行。过不了 → 就地打磨判定逻辑。