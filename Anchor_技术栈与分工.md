# Anchor — 技术栈 & 25天/2人分工

## 1. 技术栈

| 模块 | 技术 | 说明 |
|---|---|---|
| 插件形态 | Chrome 扩展，Manifest V3 | load unpacked，demo 不需要上架审核 |
| 前端 UI | React + Tailwind + shadcn/ui | side panel / content script 浮层渲染桌宠和设置界面 |
| 信号采集 | `chrome.tabs`、`chrome.idle`、Page Visibility API | 全部是扩展权限内的原生能力，零后端成本 |
| 检测引擎 | 纯 TypeScript，独立模块，不依赖 Chrome API | 输入信号→输出置信度，可单独测试；**这是简历上最值钱的部分**，务必和插件壳解耦 |
| 状态机 | 手写状态机，或引入 XState | 用状态机建模"陪伴中/观察中/check-in中"这几种状态，比一堆 if-else 更好讲清楚设计思路 |
| LLM 调用 | Anthropic API | 用途仅两处：①任务拆解出第一步建议（单次调用，不做多轮对话）②域名-任务相关性分类（惰性调用+缓存） |
| 分类缓存 | `chrome.storage.local` 或简单内存 Map | 每个域名每会话只判一次，减少 API 调用，可以量化成一个数字写进 Q&A 弹药库 |
| 后端/同步 | Supabase（Postgres + Realtime） | 存任务/会话/锚点/白名单/check-in 反应；后续手机同步、悬浮窗状态订阅都复用这一层 |
| 悬浮呈现（第二阶段） | `documentPictureInPicture` API | 纯浏览器原生能力，不需要 Electron/Tauri，核心逻辑跑稳后再加 |
| 手机 companion（可选，若有余量） | 极简 PWA，只监听自己是否在前台 | 不做"检测在刷什么"，只做"人是否离开手机可视范围"这一个粗信号 |

---

## 2. 两人分工时间安排（8/21–9/15，25天）

结构分两个阶段：**核心部分两人一起做**（风险最高、最容易出方向性偏差，合著比分头做再合并更快），**核心跑稳后再拆开并行**（这时候剩下的都是边界清晰、互不依赖的模块，分头做效率更高）。

### 阶段一（Day 1–12）：两人共同攻克检测引擎 + 端到端最小闭环

不建议这阶段就分"你做 A 我做 B"，因为这部分所有环节互相牵动（信号定义变了、评分逻辑就得跟着变），分头做很容易返工。建议采用"结对编程"或"每天对齐产出"的方式：

- **Day 1–2**：一起定义 `Signal` 接口和 `DetectionResult` 接口（这是后面所有代码的契约，两人现场过一遍达成一致，避免后面对不上）
- **Day 3–5**：一起写检测引擎核心逻辑（纯 TS 模块，见上节架构说明），用 mock 数据跑通评分函数——可以一人写评分逻辑，另一人同步写测试用例（拿真实场景编几十组 mock 信号，验证误报/漏报），两人节奏保持同步，不要一人写完另一人才开始看
- **Day 6–8**：一起接入真实 Chrome 信号（`chrome.tabs`/`chrome.idle`/Visibility），替换 mock，跑通"插件里真实触发一次 check-in"的完整闭环——这一步经常有平台 API 的坑（权限、生命周期），两人一起攻更快
- **Day 9–10**：一起接入功能一最小版（单次 LLM 调用生成第一步建议），确认它产出的任务声明/锚点数据能正确喂给检测引擎——这是两个功能的接口，必须两人都清楚
- **Day 11–12**：一起用真实使用场景（各自写代码、查资料）反复测试，调阈值，这是决定 demo 成败的核心环节，两人一起测比一人测更容易发现遗漏场景

**阶段一结束的检查点（硬性）**：能不能稳定复现两个反差瞬间（疯狂切 tab 不打扰、飘去无关网站触发 check-in）。跑不稳，不进入阶段二，继续加时间打磨。

### 阶段二（Day 13–25）：核心稳定后拆开并行

这时候接口已经稳定（`Signal` → `DetectionResult`），剩下的模块彼此独立，适合拆开：

**人 A：后端 + 检测引擎深化**
- Day 13–15：Supabase 存储接入（任务/会话/白名单/check-in 反应表）
- Day 16–18：LLM 域名分类缓存优化、自适应退让的单会话启发式逻辑
- Day 19–21：（如果有余量）`documentPictureInPicture` 悬浮窗接入
- Day 22–25：联调、修 bug、准备 Q&A 技术细节（误报率数字、缓存命中率）

**人 B：UI + 呈现层 + Demo**
- Day 13–16：桌宠动画完善（陪伴中/观察中/check-in 几个状态）、check-in 交互 UI
- Day 17–19：视觉打磨、check-in 措辞反复调整（要像朋友，不像监工）
- Day 20–22：收尾反思视图、Dashboard 细节
- Day 23–25：demo 脚本排练、录制备用视频（防止现场信号触发不稳定）

阶段二两人仍建议每天花 15-20 分钟对齐一下进度，避免各自封闭开发到最后才发现接口不匹配。


